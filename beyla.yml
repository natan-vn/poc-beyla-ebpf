log_level: INFO

discovery:
  instrument:
    - open_ports: 1337
      name: perlapp
      namespace: poc-ebpf

attributes:
  heuristic_sql_detect: true

otel_traces_export:
  endpoint: http://grafana:4318
otel_metrics_export:
  endpoint: http://grafana:4318
